
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<table class="table-bordered table-striped">
   
    @for (int y = 1; y < 11; y++)
    {
        <tr>
           
            @for (int x = 1; x < 11; x++)
            {
                <td>@(y*x) </td>
            }
        </tr>
    }
</table>